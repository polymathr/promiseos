import { AppShell } from "@/components/AppShell";
import { Button } from "@/components/ui/button";
import { CheckCheck, CircleHelp, Clock3 } from "lucide-react";
import { useState } from "react";
import { Link, useRoute } from "wouter";
export default function PromiseDetail() { const [, params] = useRoute("/promises/:id"); const [accepted, setAccepted] = useState(false); return <AppShell><Link href="/promises" className="text-sm font-extrabold text-[#1f5558]">← All promises</Link><section className="mx-auto mt-5 max-w-3xl rounded-[1.35rem] border bg-white p-6 shadow-[0_12px_28px_rgba(30,42,50,.06)]"><span className="rounded-full bg-[#f5e8e3] px-2.5 py-1 text-[11px] font-extrabold text-[#7d493b]">renegotiation proposed</span><h1 className="mt-4 text-3xl font-extrabold tracking-[-.045em]">Share the onboarding materials</h1><p className="mt-3 text-sm leading-6 text-[#66737b]">New research collaborator onboarding. Completion means the welcome deck and access links are shared.</p><div className="mt-5 rounded-xl bg-[#f6f7f5] p-4 text-sm"><p className="font-extrabold">Ren Ito → You</p><p className="mt-2 text-xs font-bold text-[#526a6b]"><Clock3 className="mr-1 inline h-3.5 w-3.5" />Friday · 11:00 AM</p></div><div className="mt-6"><p className="text-xs font-extrabold uppercase tracking-[.12em] text-[#7d493b]">Your decision</p><div className="mt-3 flex flex-wrap gap-2"><Button onClick={() => setAccepted(true)} className="rounded-xl bg-[#1f5558] text-white hover:bg-[#18484b]">Accept plan</Button><Button variant="outline" className="rounded-xl">Suggest time</Button><Button variant="outline" className="rounded-xl"><CircleHelp className="mr-1 h-4 w-4" />Ask for clarity</Button></div>{accepted && <div className="confirmation-feedback mt-4 flex items-center gap-2 rounded-xl bg-[#e8f0ea] px-4 py-3 text-sm font-extrabold text-[#3f6b50]"><CheckCheck className="h-4 w-4" />You both have the same active plan.</div>}</div><ol className="mt-8 border-l border-[#dde5e2] pl-5 text-sm"><li className="relative pb-5"><span className="absolute -left-[22px] top-1 h-2 w-2 rounded-full bg-[#1f5558]" /><strong>New plan proposed</strong><p className="mt-1 text-xs text-[#66737b]">Ren asked to move the materials to Friday morning.</p></li><li className="relative"><span className="absolute -left-[22px] top-1 h-2 w-2 rounded-full bg-[#a96f26]" /><strong>Promise confirmed</strong><p className="mt-1 text-xs text-[#66737b]">You and Ren agreed on the initial commitment.</p></li></ol><p className="mt-8 text-xs text-[#7a898b]">Promise reference: {params?.id}</p></section></AppShell>; }
